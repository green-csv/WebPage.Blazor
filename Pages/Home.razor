@page "/"
@inherits BootInteropComponentBase
<PageTitle>Home</PageTitle>

<div class="flex gap-4 mb-4">
    <button class="@GetTabClass("projects")" @onclick="@(() => SelectTab("projects"))">
        <span class="crt-wiggle  text-lime-400 text-shadow-terminal-teal">
            [
        </span>
        Projects
        <span class="crt-wiggle  text-lime-400 text-shadow-terminal-teal">
            ]
        </span>

    </button>
    <button class="@GetTabClass("contact")" @onclick="@(() => SelectTab("contact"))">
        <span class="crt-wiggle text-lime-400 text-shadow-terminal-teal">
                [
        </span>
        Contact
        <span class="crt-wiggle  text-lime-400 text-shadow-terminal-teal">
                ]
        </span>
    </button>
</div>
<hr class="shadow-md my-4 h-[3px] bg-teal-400 rounded shadow-teal-200" />
<div class="px-4 font-mono text-lime-300 text-sm">
    @if (_activeTab == "projects")
    {
        <p>> Displaying PROJECTS screen...</p>
        <!-- Inject projects content here -->
    }
    else if (_activeTab == "contact")
    {
        <p>> Displaying CONTACT screen...</p>
        <!-- Inject contact content here -->
    }
</div>

@code {
    private string _activeTab = "projects";

    private void SelectTab(string tab)
    {
        _activeTab = tab;
    }

    private string GetTabClass(string tab) =>
        $"px-4 py-2 font-mono text-sm transition-all duration-200" +
        (_activeTab == tab
            ? " bg-teal-600 text-teal-900 text-shadow-terminal-teal-darker"
            : " text-lime-300 hover:bg-teal-600 text-shadow-terminal-teal");
}